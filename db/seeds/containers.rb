puts("Seeding Containers...")

Container.find_or_initialize_by_name("10 Inch Pie Plate").tap do |container|
  container.name  = "10 Inch Pie Plate"

  container.save!()
end

Container.find_or_initialize_by_name("Gallon Freezer Bag").tap do |container|
  container.name  = "Gallon Freezer Bag"

  container.save!()
end

Container.find_or_initialize_by_name("Quart Freezer Bag").tap do |container|
  container.name  = "Quart Freezer Bag"

  container.save!()
end

Container.find_or_initialize_by_name("13x9x2 inch Pyrex Pan").tap do |container|
  container.name  = "13x9x2 inch Pyrex Pan"

  container.save!()
end

Container.find_or_initialize_by_name("11x7x1 1/2 inch Pyrex Pan").tap do |container|
  container.name  = "11x7x1 1/2 inch Pyrex Pan"

  container.save!()
end

Container.find_or_initialize_by_name("Feet Aluminum Foil").tap do |container|
  container.name  = "Feet Aluminum Foil"

  container.save!()
end

Container.find_or_initialize_by_name("9x5x3 inch Loaf Pan").tap do |container|
  container.name  = "9x5x3 inch Loaf Pan"

  container.save!()
end

Container.find_or_initialize_by_name("Sandwich Bag").tap do |container|
  container.name  = "Sandwich Bag"

  container.save!()
end

Container.find_or_initialize_by_name("8x8x2 inch Baking Dish").tap do |container|
  container.name  = "8x8x2 inch Baking Dish"

  container.save!()
end

Container.find_or_initialize_by_name("4 Cup Freezer Container").tap do |container|
  container.name  = "4 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("3 Cup Freezer Container").tap do |container|
  container.name  = "3 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("6 Cup Freezer Container").tap do |container|
  container.name  = "6 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("8 Cup Freezer Container").tap do |container|
  container.name  = "8 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("1 Cup Freezer Container").tap do |container|
  container.name  = "1 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("9 inch Pie Plate").tap do |container|
  container.name  = "9 inch Pie Plate"

  container.save!()
end

Container.find_or_initialize_by_name("9.5 Cup Freezer Container").tap do |container|
  container.name  = "9.5 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("2.5 Cup Freezer Container").tap do |container|
  container.name  = "2.5 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("15 Cup Freezer Container").tap do |container|
  container.name  = "15 Cup Freezer Container"

  container.save!()
end

Container.find_or_initialize_by_name("3 1/4 Cup Round").tap do |container|
  container.name  = "3 1/4 Cup Round"

  container.save!()
end

Container.find_or_initialize_by_name("6 Cup Flat").tap do |container|
  container.name  = "6 Cup Flat"

  container.save!()
end