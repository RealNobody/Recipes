- if flash.keys.length > 0
  .recipe-error-hidden
    .alert.alert-error.recipe-alert
      %button.close{ type: "button", data: { dismiss: "alert" } } &times;
      %strong Error!
      The following errors have occured:
      %br/
      - flash.each do |key, value|
        %div{ class: "alert alert-#{key}"}= value
= yield