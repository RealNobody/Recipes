<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Recipies</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= csrf_meta_tags %>
    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="recipie-body">
      <%= render "shared/header" %>
      <div class="recipie-container">
        <div class="container-fluid">
          <div class="row-fluid">
            <div class="span2">
              <div class="well sidebar-nav">
                <ul class="nav nav-list">
                  <li class="nav-header">Standard Functions</li>
                  <li><%= link_to("Menus", root_path) %></li>
                  <li><%= link_to("Recipies", root_path) %></li>
                  <li class="nav-header">Administrative Functions</li>
                  <li><%= link_to("Measuring Units", measuring_units_path) %></li>
                </ul>
              </div>
            </div>
            <div class="span10">
              <div class="container-fluid">
                <% if flash.keys.length > 0 %>
                  <div class="row-fluid">
                    <div class="span12">
                      <div class="alert alert-errro">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>Error!</strong> The following errors have occured:<br />
                        <% flash.each do |key, value| %>
                          <div class="alert alert-<%= key %>"><%= value %></div>
                        <% end %>
                      </div>
                    </div>
                  </div>
                <% end %>
                <div class="row-fluid">
                  <div class="span12">
                    <%= yield %>
                  </div>
                </div>
                <div class="row-fluid">
                  <div class="span12">
                    <%= debug(params) if Rails.env.development? %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <%= render "shared/footer" %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>